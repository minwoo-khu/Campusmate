# CampusMate Web App

`apps/web_app`은 Flutter 기반 웹 런처입니다.  
공통 기능 코드는 `../../packages/campusmate_core`를 사용합니다.

## 로컬 실행

```powershell
flutter run -d chrome
```

## 릴리즈 빌드

```powershell
flutter build web --release
```

출력 파일:

- `build/web`
